[pytest]
# Guard the coverage the suite actually provides (total ~65%). The floor sits a
# little below that so ordinary variation does not fail a run, while a real drop
# still does. Raise it as coverage grows.
addopts = -ra --cov-fail-under=60
norecursedirs =
    .git
    .venv
    dist
    build
    .github
